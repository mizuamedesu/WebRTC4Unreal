{
  "name": "webrtc4unreal-flow-credential-broker",
  "version": "1.0.0",
  "private": true,
  "description": "Trusted short-lived Flow credential and session broker for WebRTC4Unreal.",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "check": "node --check server.js",
    "test": "node --test"
  }
}
